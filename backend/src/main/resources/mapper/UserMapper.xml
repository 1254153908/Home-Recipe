<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.huhu.recipe.auth.mapper.UserMapper">

    <select id="selectByEmail" resultType="org.huhu.recipe.auth.entity.User">
        SELECT id, username, email, password_hash, nickname, avatar_url, created_at
        FROM user
        WHERE email = #{email}
    </select>

    <select id="selectByUsername" resultType="org.huhu.recipe.auth.entity.User">
        SELECT id, username, email, password_hash, nickname, avatar_url, created_at
        FROM user
        WHERE username = #{username}
    </select>

</mapper>
