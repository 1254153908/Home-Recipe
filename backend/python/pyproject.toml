[project]
name = "homerecipe-ai"
version = "1.0.0"
description = "HomeRecipe AI recognition service"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
