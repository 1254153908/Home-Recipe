# HomeRecipe AI Service Configuration

server:
  host: "0.0.0.0"
  port: 5000

ai:
  provider: openai
  mock_enabled: false

logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
